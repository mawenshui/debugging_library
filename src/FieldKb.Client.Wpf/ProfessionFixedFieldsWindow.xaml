<local:ThemedWindow x:Class="FieldKb.Client.Wpf.ProfessionFixedFieldsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FieldKb.Client.Wpf"
        Title="职业设置" Height="620" Width="980" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="14" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource Card}" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="职业设置" />
                    <TextBlock Margin="0,4,0,0" Foreground="{StaticResource Brush.SubText}" Text="选择职业并配置“默认固定字段”（最多 8 个）。" TextWrapping="Wrap" />
                </StackPanel>

                <ListBox Grid.Row="1" Margin="0,12,0,0"
                         ItemsSource="{Binding Professions}"
                         DisplayMemberPath="DisplayName"
                         SelectedValuePath="Id"
                         SelectedValue="{Binding SelectedProfessionId}" />
            </Grid>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource Card}" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock FontWeight="SemiBold" Text="默认固定字段" />
                    <TextBlock Grid.Column="1" Foreground="{StaticResource Brush.SubText}" Text="{Binding SelectedCountText}" />
                </Grid>

                <Grid Grid.Row="1" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Height="40"
                             Tag="新增固定字段：输入字段名称（例如：PLC 固件）"
                             Text="{Binding NewFieldName, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="2" Width="120" Content="添加" Command="{Binding AddFieldCommand}" />
                </Grid>

                <ScrollViewer Grid.Row="2" Margin="0,12,0,0" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding FieldOptions}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Margin="0,0,12,10"
                                          IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                          Content="{Binding DisplayName}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Grid Grid.Row="3" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock VerticalAlignment="Center" Foreground="{StaticResource Brush.SubText}" Text="{Binding StatusText}" TextWrapping="Wrap" />
                    <Button Grid.Column="1" Width="110" Style="{StaticResource Button.Outline}" Content="取消" Command="{Binding CancelCommand}" />
                    <Button Grid.Column="3" Width="110" Content="保存" Command="{Binding SaveCommand}" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</local:ThemedWindow>
