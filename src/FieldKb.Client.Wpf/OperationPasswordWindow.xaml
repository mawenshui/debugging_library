<local:ThemedWindow x:Class="FieldKb.Client.Wpf.OperationPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FieldKb.Client.Wpf"
        Title="操作密码" Height="360" Width="520" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Border Style="{StaticResource Card}" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="操作密码" />
                    <TextBlock Margin="0,4,0,0" Foreground="{StaticResource Brush.SubText}" Text="用于执行批量删除等高危操作。本机保存密码的哈希，不保存明文。" TextWrapping="Wrap" />
                </StackPanel>

                <TextBlock Grid.Row="1" Margin="0,14,0,0" FontWeight="SemiBold" Text="新密码" />
                <PasswordBox Grid.Row="2" Height="40" Margin="0,8,0,0"
                             local:PasswordBoxBinding.BindPassword="True"
                             local:PasswordBoxBinding.BoundPassword="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="3" Margin="0,14,0,0" FontWeight="SemiBold" Text="确认密码" />
                <PasswordBox Grid.Row="4" Height="40" Margin="0,8,0,0"
                             local:PasswordBoxBinding.BindPassword="True"
                             local:PasswordBoxBinding.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
        </Border>

        <Grid VerticalAlignment="Bottom" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Foreground="{StaticResource Brush.SubText}" Text="{Binding StatusText}" TextWrapping="Wrap" />
            <Button Grid.Column="1" Width="110" Style="{StaticResource Button.Outline}" Content="取消" Command="{Binding CancelCommand}"/>
            <Button Grid.Column="3" Width="110" Content="保存" Command="{Binding SaveCommand}"/>
        </Grid>
    </Grid>
</local:ThemedWindow>
