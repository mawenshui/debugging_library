<local:ThemedWindow x:Class="FieldKb.Client.Wpf.SpreadsheetImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FieldKb.Client.Wpf"
        Title="表单导入" Height="640" Width="920" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Card}" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="表单导入" />
                    <TextBlock Margin="0,6,0,0" Foreground="{StaticResource Brush.SubText}"
                               Text="支持导入由本系统导出的 Excel（.xlsx）或 Excel+附件包（.zip）。仅 .xlsx 不包含附件内容，建议优先使用 .zip。"
                               TextWrapping="Wrap" />
                </StackPanel>

                <Grid Grid.Row="1" Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Height="40" IsReadOnly="True" Text="{Binding ImportPath}" />
                    <Button Grid.Column="2" Width="120" Style="{StaticResource Button.Outline}" Content="选择文件" Command="{Binding PickFileCommand}" />
                    <Button Grid.Column="4" Width="110" Style="{StaticResource Button.Outline}" Content="预检" Command="{Binding PreviewCommand}" />
                </Grid>

                <Grid Grid.Row="2" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" Text="冲突策略" />
                        <ComboBox Height="40" Margin="0,8,0,0"
                                  ItemsSource="{Binding ConflictPolicyOptions}"
                                  DisplayMemberPath="DisplayName"
                                  SelectedItem="{Binding SelectedConflictPolicy, Mode=TwoWay}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock FontWeight="SemiBold" Text="标签合并" />
                        <ComboBox Height="40" Margin="0,8,0,0"
                                  ItemsSource="{Binding TagMergeModeOptions}"
                                  DisplayMemberPath="DisplayName"
                                  SelectedItem="{Binding SelectedTagMergeMode, Mode=TwoWay}" />
                    </StackPanel>
                </Grid>

                <TextBox Grid.Row="3" Margin="0,14,0,0" Padding="12"
                         Text="{Binding PreviewText}"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Foreground="{StaticResource Brush.SubText}" Text="{Binding StatusText}" TextWrapping="Wrap" />
            <Button Grid.Column="1" Width="110" Style="{StaticResource Button.Outline}" Content="关闭" Command="{Binding CloseCommand}" />
            <Button Grid.Column="3" Width="130" Style="{StaticResource Button.Outline}" Content="预检" Command="{Binding PreviewCommand}" />
            <Button Grid.Column="5" Width="130" Content="导入" Command="{Binding ImportCommand}" />
        </Grid>
    </Grid>
</local:ThemedWindow>
