<local:ThemedWindow x:Class="FieldKb.Client.Wpf.DataPurgeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FieldKb.Client.Wpf"
        Title="批量删除（危险）" Height="720" Width="980" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Card}" Padding="14">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="批量删除（危险）" />
                        <TextBlock Margin="0,4,0,0" Foreground="{StaticResource Brush.SubText}"
                                   Text="本功能为物理删除，无法恢复。支持按职业/标签/更新时间范围筛选后批量删除。" TextWrapping="Wrap" />
                    </StackPanel>

                    <Grid Grid.Row="1" Margin="0,14,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="14" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="14" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock FontWeight="SemiBold" Text="职业筛选" />
                            <ComboBox Height="40" Margin="0,8,0,0"
                                      ItemsSource="{Binding ProfessionOptions}"
                                      DisplayMemberPath="DisplayName"
                                      SelectedValuePath="Id"
                                      SelectedValue="{Binding SelectedProfessionFilterId, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock FontWeight="SemiBold" Text="更新时间（起）" />
                            <DatePicker Height="40" Margin="0,8,0,0"
                                        SelectedDate="{Binding UpdatedFromDate, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Grid.Column="4">
                            <TextBlock FontWeight="SemiBold" Text="更新时间（止）" />
                            <DatePicker Height="40" Margin="0,8,0,0"
                                        SelectedDate="{Binding UpdatedToDate, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>

                    <CheckBox Grid.Row="2" Margin="0,12,0,0"
                              Content="包含已软删除记录（isDeleted=1）"
                              IsChecked="{Binding IncludeSoftDeleted, Mode=TwoWay}" />

                    <StackPanel Grid.Row="3" Margin="0,14,0,0">
                        <TextBlock FontWeight="SemiBold" Text="标签筛选（可多选）" />
                        <ScrollViewer Margin="0,8,0,0" Height="220" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Tags}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Margin="0,0,12,10"
                                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                  Content="{Binding Name}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </StackPanel>

                    <StackPanel Grid.Row="4" Margin="0,14,0,0">
                        <TextBlock FontWeight="SemiBold" Text="操作密码验证" />
                        <PasswordBox Height="40" Margin="0,8,0,0"
                                     local:PasswordBoxBinding.BindPassword="True"
                                     local:PasswordBoxBinding.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Foreground="{StaticResource Brush.SubText}"
                                   Text="输入本机操作密码后才能执行删除。" TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="1" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock VerticalAlignment="Center" Foreground="{StaticResource Brush.SubText}" Text="{Binding StatusText}" TextWrapping="Wrap" />
            <Button Grid.Column="1" Width="130" Style="{StaticResource Button.Outline}" Content="预览数量" Command="{Binding RefreshCountCommand}" />
            <Button Grid.Column="3" Width="110" Style="{StaticResource Button.Outline}" Content="关闭" Command="{Binding CloseCommand}" />
            <Button Grid.Column="5" Width="170" Content="{Binding PreviewCount}" ContentStringFormat="删除（{0}）" Command="{Binding DeleteCommand}" />
        </Grid>
    </Grid>
</local:ThemedWindow>
